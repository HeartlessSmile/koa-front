/**
 * 根据选区，计算定位（用于 modal hoverbar）
 * @param editor editor
 */
export function getPositionBySelection(editor) {
  
  let positionStyle = {
    left: 0,
    top: 0,
    width: 0,
    height: 0,
  }

  return positionStyle
}
